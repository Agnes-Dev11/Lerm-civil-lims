


services:
  postgres:
    image: postgres
    environment:
      POSTGRES_DB: lermcivil
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: password
    ports:
      - "5432:5432"

  app:
    build: .
    ports:
      - "8080:8080"
    depends_on:
      - postgres
